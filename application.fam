# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="hello_flipper_zero",  # Must be unique
    name="App hello_flipper_zero",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="hello_flipper_zero_app",
    stack_size=2 * 1024,
    fap_category="Examples",
    # Optional values
    # fap_version="0.1",
    fap_icon="hello_flipper_zero.png",  # 10x10 1-bit PNG
    # fap_description="A simple app",
    # fap_author="J. Doe",
    # fap_weburl="https://github.com/user/hello_flipper_zero",
    fap_icon_assets="images",  # Image assets to compile for this application
)
